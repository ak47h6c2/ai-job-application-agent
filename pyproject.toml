[project]
name = "ai-job-application-agent"
version = "0.1.0"
description = "An AI-assisted job application workflow for parsing job emails and matching roles against a resume profile."
requires-python = ">=3.11"
dependencies = [
  "pypdf>=5.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
